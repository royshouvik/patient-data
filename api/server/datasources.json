{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongodb": {
    "host": "mongodb",
    "port": 27017,
    "url": "${MONGODB_URL}",
    "database": "database",
    "password": "${MONGO_PASS}",
    "name": "mongodb",
    "user": "${MONGO_USER}",
    "connector": "mongodb"
  }
}
